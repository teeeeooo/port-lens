{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-drag",
  "description": "Allow native window dragging only from the main Port Lens window",
  "windows": ["main"],
  "permissions": ["core:window:allow-start-dragging"]
}
